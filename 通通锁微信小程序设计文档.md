# 智能锁微信小程序设计文档

## 〇、设计原则（最重要）

> **⚠️ 核心原则：用户100%不知道通通锁的存在**
>
> 1. 所有UI/UX中不得出现"通通锁"、"TTLock"、"Sciener"等任何品牌标识
> 2. 用户只感知自己的账号（手机号/邮箱），通通锁账号由后端自动创建/管理
> 3. 用户只看到"智能锁"、"电子钥匙"、"密码"等直观概念，无任何TTLock术语
> 4. 即使发送电子钥匙给他人，接收方也只用本系统的手机号/邮箱，后端自动映射为TTLock用户名
> 5. 小程序名称、图标、品牌均为自有品牌，与通通锁无关

---

## 一、系统架构概述

### 1.1 整体架构

```
┌────────────────────────────────────────────────────────┐
│                    微信小程序（自有品牌）                │
│  ┌──────────┐  ┌──────────┐  ┌────────────────────┐   │
│  │ 用户界面   │  │ 业务逻辑   │  │ TTLock微信插件     │   │
│  │ (WXML)    │→│ (JS/TS)   │→│ (蓝牙通信-对内)    │   │
│  └──────────┘  └──────────┘  └────────────────────┘   │
│                      │                                  │
│                      ↓ HTTP/HTTPS                       │
│              ┌────────────────┐                         │
│              │   本系统后端     │ ←── 用户只与此交互     │
│              │   (REST API)   │                         │
│              └───────┬────────┘                         │
│                      │ (后端内部调用, 用户无感知)        │
│                      ↓ HTTP/HTTPS                       │
│              ┌────────────────┐                         │
│              │  TTLock Cloud  │ ←── 基础设施, 用户不可见 │
│              │   API V3      │                         │
│              └────────────────┘                         │
└─────────────────────────────────────────────────────────┘
```

### 1.2 核心设计理念

| 理念 | 说明 |
|------|------|
| **完全替代** | 本小程序=通通锁app的替代品，用户只需装这一个即可 |
| **零感知** | 用户无需注册/登录/了解通通锁，一切都是后端自动处理 |
| **自有用户体系** | 独立的注册登录系统，手机号/邮箱即可使用 |
| **后端代理** | 所有TTLock API调用由后端封装，小程序只调本系统接口 |

### 1.3 数据关联（后端视角，用户不可见）

```
本系统用户(1) ──→ (1) TTLock用户 ←── 注册时后端自动创建，用户不知晓
本系统用户(1) ──→ (N) 锁设备 (通过lockId关联)
本系统用户(1) ──→ (N) 电子钥匙 (通过keyId关联)
本系统用户(1) ──→ (N) 离线密码 (通过keyboardPwdId关联)
```

---

## 二、涉及的通通锁 Cloud API V3 接口清单（后端使用，对内不对外）

> **基础地址**: `https://cnapi.ttlock.com`
> **Content-Type**: `application/x-www-form-urlencoded`
> **使用方**: 仅本系统后端调用，小程序端不直接调用任何TTLock API

### 2.1 用户管理接口

#### ① 新用户注册
- **接口**: `POST /v3/user/register`
- **触发时机**: 用户在本系统注册成功后，后端自动调用
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | 开放平台分配的app_id |
| clientSecret | String | Y | 开放平台分配的app_secret |
| username | String | Y | 使用本系统用户ID（规则: `${envPrefix}${userId}`，环境前缀+用户ID，TTLock自动添加平台前缀） |
| password | String | Y | 密码（32位小写MD5加密后传输） |
| date | Long | Y | 当前时间戳（毫秒） |

- **返回参数**: `{ "username": "prefix_app_${userId}" }`
- **⚠️ 关键注意**:
  - TTLock 会自动给用户名加前缀（如 `gegjb_`），返回的 `username` 是带前缀的完整用户名
  - 后续所有 TTLock API 调用（获取token、调用锁接口等）必须使用**返回的带前缀用户名**，而非本地生成的用户名
  - 密码必须是 **32位小写 MD5** 加密后传输
- **存储方式**: 返回的username（带前缀）和生成的password加密存入数据库`user.tt_username`和`user.tt_password`，**对外开放的API返回值中不得包含这些字段**

#### ② OAuth2 令牌获取与刷新机制

TTLock Cloud API V3 使用 OAuth2 鉴权，每个用户的每次 API 调用都需要携带 `accessToken`。本系统后端采用 **按用户独立管理令牌** 的策略，实现自动获取、存储和续期。

> **⚠️ 关键约束**:
> - 所有 TTLock API 的密码参数均需 **32位小写 MD5** 加密后传输
> - 注册时 TTLock 返回的用户名带自动前缀（如 `gegjb_`），后续所有接口调用必须使用带前缀的用户名
> - 注册和获取token必须成功，失败应抛异常回滚用户注册流程

##### 令牌生命周期

```
用户注册成功
    │
    ▼
[初次获取] POST /oauth2/token (grant_type=password)
    → 返回 access_token + refresh_token + expires_in(90天)
    → 存入 user.tt_access_token, tt_refresh_token, tt_token_expires_at
    │
    ▼
[正常调用] 每次调用 TTLock API 时：
    1. 检查 tt_token_expires_at 是否在 24 小时内过期
    2. 若即将过期 → 触发刷新
    3. 若已过期 → 使用 tt_password 重新获取
    4. 携带 access_token 正常调用
    │
    ▼
[刷新令牌] POST /oauth2/token (grant_type=refresh_token)
    → 返回新的 access_token + refresh_token + expires_in
    → 更新数据库
    │
    ▼
[令牌失效] 若 refresh_token 也过期（极少发生，有效期90天）
    → 回退到使用 tt_username + tt_password 重新获取
```

##### 初次获取令牌（用户注册后触发）

- **接口**: `POST /oauth2/token`
- **触发时机**: TTLock 用户注册成功后的首次调用
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| client_id | String | Y | 开放平台分配的 app_id |
| client_secret | String | Y | 开放平台分配的 app_secret |
| username | String | Y | TTLock 用户名（即 tt_username） |
| password | String | Y | TTLock 密码（32位小写MD5加密，即 tt_password 的MD5值） |

- **返回参数**:
```json
{
  "access_token": "xxx",
  "uid": 123,
  "refresh_token": "yyy",
  "expires_in": 7776000
}
```
- **后端处理**:
  - `access_token` → 存入 `user.tt_access_token`
  - `refresh_token` → 存入 `user.tt_refresh_token`
  - `expires_in`(秒) + 当前时间 → 计算过期时间戳存入 `user.tt_token_expires_at`
  - `uid` → 存入 `user.tt_uid`

##### 刷新令牌机制

- **接口**: `POST /oauth2/token`
- **触发时机**: access_token 将在 24 小时内过期时（后端自动判断并刷新）
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| client_id | String | Y | app_id |
| client_secret | String | Y | app_secret |
| grant_type | String | Y | 固定值 `refresh_token` |
| refresh_token | String | Y | 之前获取并存储的 refresh_token |
| username | String | Y | TTLock 用户名 |

- **返回参数**: 与初次获取相同 `{ "access_token", "uid", "refresh_token", "expires_in" }`
- **后端处理**: 更新数据库中该用户的三个令牌字段

##### 令牌管理核心逻辑（后端伪代码）

```java
// 获取有效的 accessToken（每次调用 TTLock API 前调用）
String getValidAccessToken(Long userId) {
    User user = userDao.findById(userId);
    
    // 检查是否首次或令牌已过期（无 refresh_token 兜底）
    if (user.getTtAccessToken() == null || isTokenExpired(user)) {
        // refresh_token 有效 → 尝试刷新
        if (user.getTtRefreshToken() != null && !isRefreshTokenExpired(user)) {
            return refreshToken(user);
        }
        // refresh_token 也无效 → 重新用密码获取
        return acquireToken(user);
    }
    
    return user.getTtAccessToken();
}

// 判断 access_token 是否即将过期（提前 24 小时刷新）
boolean isTokenExpired(User user) {
    return System.currentTimeMillis() >= user.getTtTokenExpiresAt() - 24 * 60 * 60 * 1000L;
}

// 判断 refresh_token 是否过期（提前 7 天刷新）
boolean isRefreshTokenExpired(User user) {
    // refresh_token 未单独返回 expires_in，假设与 access_token 同时过期
    // 保守策略：距过期时间 < 7 天时认为 refresh_token 不可用，走密码重新获取
    return System.currentTimeMillis() >= user.getTtTokenExpiresAt() - 7 * 24 * 60 * 60 * 1000L;
}
```

### 2.2 锁管理接口

#### ④ 锁初始化
- **接口**: `POST /v3/lock/initialize`
- **触发时机**: 用户在小程序通过蓝牙添加新锁后，后端将lockData同步到云端
- **说明**: 与微信小程序插件 `initLock` 配套使用，接收插件返回的组合 `lockData` 字符串。另有 `/v3/lock/init`（参数更细，需lockKey/aesKeyStr等）供原生SDK使用，本系统统一用 `/v3/lock/initialize`
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| lockData | String | Y | 蓝牙插件initLock成功后返回的lockData |
| lockAlias | String | N | 锁别名（用户自定义名称） |
| date | Long | Y | 当前时间戳（毫秒） |

- **返回参数**: `{ "lockId": 123456, "keyId": 789 }`

#### ⑤ 获取锁列表
- **接口**: `GET /v3/lock/list`
- **触发时机**: 用户打开小程序锁列表页时，后端代理调用
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| pageNo | Int | Y | 页码，从1开始 |
| pageSize | Int | Y | 每页数量，默认20，最大10000 |
| date | Long | Y | 当前时间戳（毫秒） |

- **返回参数**（后端处理后返回给小程序，只暴露必要字段）:
```json
{
  "list": [
    {
      "lockId": 123,
      "lockName": "蓝牙名称",
      "lockAlias": "用户自定义名称",
      "lockMac": "AA:BB:CC:DD:EE:FF",
      "electricQuantity": 100,
      "lockData": "xxx",
      "keyboardPwdVersion": 4,
      "specialValue": 1288,
      "hasGateway": 0
    }
  ]
}
```

#### ⑥ 获取单把锁详情
- **接口**: `GET /v3/lock/detail`
- **触发时机**: 用户查看锁详情时

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| lockId | Int | Y | 锁ID |
| date | Long | Y | 当前时间戳（毫秒） |

#### ⑦ 更新锁数据
- **接口**: `POST /v3/lock/updateLockData`
- **触发时机**: 重置钥匙/重置键盘密码/修改特征值后，将新的lockData上传到云端
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| lockId | Int | Y | 锁ID |
| lockData | String | Y | SDK生成的新lockData |
| date | Long | Y | 当前时间戳（毫秒） |

### 2.3 电子钥匙接口

#### ⑧ 获取钥匙列表
- **接口**: `GET /v3/key/list`
- **触发时机**: 用户查看"我的电子钥匙"时
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| pageNo | Int | Y | 页码 |
| pageSize | Int | Y | 每页数量 |
| date | Long | Y | 当前时间戳（毫秒） |

- **返回参数**（后端处理后）:

| 字段（暴露给小程序） | 说明 |
|---|---|
| keyId | 钥匙ID |
| lockData | 锁数据（开锁用） |
| lockId | 锁ID |
| keyType | 钥匙类型：`owner`=管理员, `common`=普通 |
| lockName | 锁名称 |
| lockAlias | 锁别名 |
| lockMac | 锁MAC地址 |
| electricQuantity | 电量 |
| startDate | 有效期开始 |
| endDate | 有效期结束 |
| remarks | 备注 |
| remoteEnable | 是否支持远程开锁（⚠️ 本期暂不实现，字段保留） |

#### ⑨ 获取单把钥匙（含下载钥匙数据）
- **接口**: `GET /v3/key/get`
- **触发时机**: 用户查看钥匙详情或需要获取 `lockData` 用于蓝牙开锁时
- **说明**: V3 接口直接返回 `lockData`，可替代 V2 的 `/v2/key/download` 接口。管理员和普通用户均可调用

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| lockId | Int | Y | 锁ID |
| date | Long | Y | 当前时间戳（毫秒） |

#### ⑩ 发送电子钥匙
- **接口**: `POST /v3/key/send`
- **触发时机**: 锁拥有者向其他用户发送钥匙时
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| lockId | Int | Y | 锁ID |
| receiverUsername | String | Y | **接收方的TTLock用户名（后端根据手机号/邮箱查到映射）** |
| keyName | String | Y | 钥匙名称 |
| startDate | Long | Y | 有效期开始 |
| endDate | Long | Y | 有效期结束 |
| remarks | String | N | 备注 |
| remoteEnable | Int | N | 远程开锁: 1-是, 2-否（⚠️ 本期暂不实现，字段保留） |
| createUser | Int | N | **1-自动创建（接收方可能还没有TTLock账号）** |
| date | Long | Y | 当前时间戳 |

- **关键设计**: `receiverUsername`参数传入的是接收方**本系统的手机号/邮箱**，配合`createUser=1`，TTLock会自动创建账号；后端跟踪此映射关系。⚠️ 接收方TTLock账号也遵循自动前缀规则，后续调用需使用带前缀的完整用户名

- **钥匙类型实现**:

| 用户选择的类型 | startDate | endDate | 说明 |
|---|---|---|---|
| 永久 | 0 | 0 | 永不过期 |
| 限时 | 开始时间戳 | 结束时间戳 | 在指定时间段内有效 |
| 单次 | 当前时间 | 当前时间+6h | 有效期短，后端记录使用次数控制 |
| 循环 | 开始时间戳 | 结束时间戳 | 固定时间段周期有效 |

#### ⑪ 删除电子钥匙
- **接口**: `POST /v3/key/delete`
- **触发时机**: 删除电子钥匙（管理员删除任意钥匙，普通用户删除自己的钥匙均用此接口）
- **说明**: V3 不区分角色，传 `keyId` 即可，无需 `lockId`。可替代 V2 的 `/v2/key/deleteBySelf`
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| keyId | Int | Y | 钥匙ID |
| date | Long | Y | 当前时间戳（毫秒） |

### 2.4 远程开/关锁接口

#### ⑫ 远程开锁 ⚠️ 暂不实现
- **接口**: `POST /v3/lock/unlock`
- **触发时机**: 用户点击远程开锁（需网关支持）
- **状态**: ⚠️ **本期暂不实现**，需网关硬件支持，后续版本考虑
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| lockId | Int | Y | 锁ID |
| date | Long | Y | 当前时间戳 |

### 2.5 密码管理接口

#### ⑬ 获取密码列表
- **接口**: `GET /v3/lock/listKeyboardPwd`
- **触发时机**: 用户查看锁的密码列表时
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| lockId | Int | Y | 锁ID |
| pageNo | Int | Y | 页码 |
| pageSize | Int | Y | 每页数量 |
| date | Long | Y | 当前时间戳 |

- **返回参数**（后端处理后）:

| 字段（暴露给小程序） | 说明 |
|---|---|
| keyboardPwdId | 密码ID |
| keyboardPwd | 密码值（敏感信息，需加密传输） |
| keyboardPwdName | 密码名称 |
| pwdType | 密码类型：1-单次 2-永久 3-限时 4-删除 5~14-循环 |
| startDate | 有效期开始 |
| endDate | 有效期结束 |
| isCustom | 是否自定义：1-是，0-随机 |

#### ⑭ 获取随机密码（离线密码）
- **接口**: `GET /v3/keyboardPwd/get`
- **触发时机**: 用户选择生成限时/单次/永久/循环/删除类型的离线密码时
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| lockId | Int | Y | 锁ID |
| keyboardPwdVersion | Int | Y | 密码版本（最新为4） |
| keyboardPwdType | Int | Y | 密码类型（见下表） |
| keyboardPwdName | String | N | 密码名称 |
| startDate | Long | N | 有效期开始 |
| endDate | Long | N | 有效期结束 |
| date | Long | Y | 当前时间戳 |

- **密码类型映射（用户界面 → API参数）**:

| 用户界面显示 | keyboardPwdType | 说明 |
|---|---|---|
| 单次 | 1 | 6小时内仅一次有效 |
| 永久 | 2 | 永久有效 |
| 限时 | 3 | 指定时间段有效 |
| 删除 | 4 | 使用后删除所有其他密码 |
| 每天 | 6 | 每天固定时段 |
| 工作日 | 7 | 工作日固定时段 |
| 周末 | 5 | 周末固定时段 |
| 周一~周日 | 8~14 | 每周某天固定时段 |

- **返回参数**: `{ "keyboardPwd": "123456", "keyboardPwdId": 234 }`

#### ⑮ 添加自定义密码
- **接口**: `POST /v3/keyboardPwd/add`
- **触发时机**: 用户通过蓝牙写入自定义密码后，同步到云端
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| lockId | Int | Y | 锁ID |
| keyboardPwd | String | Y | 密码值 |
| keyboardPwdName | String | N | 密码名称 |
| startDate | Long | Y | 有效期开始 |
| endDate | Long | Y | 有效期结束 |
| addType | Int | N | 1-蓝牙（默认） |
| date | Long | Y | 当前时间戳 |

#### ⑯ 删除密码
- **接口**: `POST /v3/keyboardPwd/delete`
- **触发时机**: 用户删除指定密码时
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| lockId | Int | Y | 锁ID |
| keyboardPwdId | Int | Y | 密码ID |
| deleteType | Int | N | 1-蓝牙(默认), 2-网关/WiFi |
| date | Long | Y | 当前时间戳 |

#### ⑰ 修改密码
- **接口**: `POST /v3/keyboardPwd/change`
- **触发时机**: 用户修改密码属性时
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| lockId | Int | Y | 锁ID |
| keyboardPwdId | Int | Y | 密码ID |
| keyboardPwdName | String | N | 新密码名称 |
| newKeyboardPwd | String | N | 新密码值 |
| startDate | Long | N | 新有效期开始 |
| endDate | Long | N | 新有效期结束 |
| changeType | Int | N | 1-蓝牙(默认), 2-网关/WiFi |
| date | Long | Y | 当前时间戳 |

#### ⑱ 重置键盘密码
- **接口**: `POST /v3/lock/resetKeyboardPwd`
- **文档地址**: `https://cnopen.ttlock.com/doc/api/v3/lock/resetKeyboardPwd`（TTLock 左侧菜单无此接口入口，需直接访问该链接）
- **触发时机**: 用户重置所有密码后，将新的密码数据上传
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| lockId | Int | Y | 锁ID |
| pwdInfo | String | Y | 蓝牙重置返回的密码数据 |
| timestamp | Long | Y | 蓝牙重置返回的时间戳 |
| date | Long | Y | 当前时间戳 |

### 2.6 操作记录接口

#### ⑲ 上传操作记录
- **接口**: `POST /v3/lockRecord/upload`
- **触发时机**: 蓝牙开锁后，将记录上传云端
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| lockId | Int | Y | 锁ID |
| records | String | Y | 蓝牙getOperationLog获取的操作记录JSON |
| date | Long | Y | 当前时间戳 |

#### ⑳ 查询操作记录
- **接口**: `GET /v3/lockRecord/list`
- **触发时机**: 用户查看开锁记录时
- **请求参数**:

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| clientId | String | Y | app_id |
| accessToken | String | Y | 访问令牌 |
| lockId | Int | Y | 锁ID |
| pageNo | Int | Y | 页码 |
| pageSize | Int | Y | 每页数量 |
| date | Long | Y | 当前时间戳 |

---

## 三、通通锁微信小程序插件接口清单（对内，蓝牙通信用）

> **插件appid**: `wx43d5971c94455481` (在线版)
> **版本**: `3.1.6`
> **引入方式**: `const plugin = requirePlugin("ttlock-plugin");`

### 3.1 调试接口

#### ① 开启/关闭调试日志
```js
plugin.setShowLog(true);
```

### 3.2 设备扫描接口

#### ② 扫描蓝牙智能锁
```js
plugin.startScanBleDevice(
  (deviceFromScan, deviceFromScanList) => {
    // deviceFromScan: 单个设备信息
    // deviceFromScanList: 所有设备列表
  },
  (res) => {
    // 扫描失败回调
  }
);
```

#### ③ 停止扫描蓝牙智能锁
```js
plugin.stopScanBleDevice();
```

### 3.3 锁操作接口

#### ④ 初始化蓝牙智能锁
```js
plugin.initLock({
  deviceFromScan: deviceFromScan,
  serverTime: Date.now(),
}).then(res => {
  // 返回 { lockData: "..." }
  // lockData需发送给后端同步到TTLock云端
}).catch(err => {});
```

#### ⑤ 控制智能锁（开锁/闭锁）
```js
// 开锁 (3=开锁, 6=闭锁)
plugin.controlLock({
  controlAction: 3,
  lockData: "xxx",           // 从后端获取的lockData
  serverTime: Date.now(),    // 服务器时间戳（建议传入，3.1.0起插件不再内部获取；不传则使用本地时间）
  disconnectCallback: (res) => {},  // 可选，设备断连回调
}).then(res => {
  // 返回 { uniqueid: 123, lockTime: 163... }
}).catch(err => {});

// 闭锁
plugin.controlLock({
  controlAction: 6,
  lockData: "xxx",
  serverTime: Date.now(),
}).then(res => {}).catch(err => {});
```

#### ⑥ 重置蓝牙智能锁（删除锁时调用）
```js
plugin.resetLock({
  lockData: "xxx",
}).then(res => {}).catch(err => {});
```

### 3.4 密码管理接口

#### ⑦ 添加自定义密码
```js
plugin.createCustomPasscode({
  passcode: "12345678",    // 4-9位数字
  startDate: "2024-01-01 00:00:00",
  endDate: "2024-12-31 23:59:59",
  lockData: "xxx",
}).then(res => {}).catch(err => {});
```

#### ⑧ 删除键盘密码
```js
plugin.deletePasscode({
  passcode: "12345678",
  lockData: "xxx",
}).then(res => {}).catch(err => {});
```

#### ⑨ 重置键盘密码
```js
plugin.resetPasscode({
  lockData: "xxx",
}).then(res => {
  // 返回 { lockData: "重置后的密码数据" }
  // 需将新lockData传给后端更新云端
}).catch(err => {});
```

#### ⑩ 读取智能锁内所有有效键盘密码
```js
plugin.getAllValidPasscode({
  lockData: "xxx",
}).then(res => {}).catch(err => {});
```

### 3.5 其他接口

#### ⑪ 读取智能锁操作记录
```js
plugin.getOperationLog({
  logType: 1,       // 1=全部, 2=未读取
  lockData: "xxx",
}).then(res => {
  // 返回 { log: "JSON字符串" }
}).catch(err => {});
```

#### ⑫ 获取智能锁版本号
```js
plugin.getLockVersion(lockData);
```

---

## 四、系统后端接口设计（小程序调用的接口）

> 所有接口返回数据中**不包含任何TTLock相关字段名**，字段名使用用户能理解的命名

### 4.1 用户模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 发送验证码 | POST | /api/user/sendCode | 发送验证码（固定返回666666） |
| 用户注册 | POST | /api/user/register | 手机号/邮箱+验证码+密码注册，**后端自动创建TTLock账号** |
| 用户登录 | POST | /api/user/login | 手机号/邮箱+密码登录，返回JWT |
| 获取用户信息 | GET | /api/user/info | 获取当前用户信息 |
| ~~搜索用户~~ | ~~GET~~ | ~~/api/user/search~~ | ~~按手机号/邮箱搜索其他用户（用于发送钥匙） — 已废弃，改为发送钥匙时直接传入接收方手机号/邮箱，由后端查询是否存在~~ |

### 4.2 锁模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 锁列表 | GET | /api/lock/list | 获取我的所有锁（我创建的+别人分享给我的） |
| 锁详情 | GET | /api/lock/detail | 获取单把锁详情（含电量、状态等） |
| 添加锁 | POST | /api/lock/add | 蓝牙初始化后，传入lockData完成添加 |
| 删除锁 | POST | /api/lock/delete | 蓝牙重置后，删除本系统和云端数据 |
| 更新锁数据 | POST | /api/lock/updateData | 重置密码后更新锁数据 |

### 4.3 电子钥匙模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 钥匙列表 | GET | /api/key/list | 获取我的所有电子钥匙 |
| 发送钥匙 | POST | /api/key/send | 给指定用户发送钥匙（按手机号/邮箱） |
| 删除钥匙 | POST | /api/key/delete | 删除指定钥匙 |
| 钥匙详情 | GET | /api/key/detail | 获取钥匙详细信息 |

### 4.4 密码模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 密码列表 | GET | /api/pwd/list | 获取锁的所有密码 |
| 获取随机密码 | POST | /api/pwd/generate | 生成指定类型的随机离线密码 |
| 添加自定义密码 | POST | /api/pwd/custom/add | 蓝牙写入后同步到云端 |
| 删除密码 | POST | /api/pwd/delete | 删除指定密码 |
| 重置密码 | POST | /api/pwd/reset | 蓝牙重置键盘密码并更新云端 |

### 4.5 操作记录模块

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 上传记录 | POST | /api/record/upload | 上传蓝牙读取的操作记录 |
| 记录列表 | GET | /api/record/list | 获取操作记录列表 |

---

## 五、数据库设计

### 5.1 用户表 (user)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| username | VARCHAR(100) | 登录账号（手机号或邮箱） |
| password | VARCHAR(255) | 密码（加密存储） |
| nickname | VARCHAR(100) | 昵称 |
| avatar | VARCHAR(500) | 头像 |
| tt_username | VARCHAR(200) | TTLock用户名（内部字段，TTLock自动加前缀如`gegjb_app...`，后续API调用必须用带前缀的完整用户名） |
| tt_password | VARCHAR(100) | TTLock密码（明文存储，用于获取token时MD5加密后传输，内部字段） |
| tt_uid | INT | TTLock用户ID（内部字段） |
| tt_access_token | VARCHAR(500) | TTLock访问令牌（内部字段，用于API调用鉴权） |
| tt_refresh_token | VARCHAR(500) | TTLock刷新令牌（内部字段，用于续期access_token） |
| tt_token_expires_at | BIGINT | TTLock令牌过期时间戳（毫秒，用于判断是否需要刷新） |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 5.2 锁表 (lock)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 所属用户ID（添加者） |
| lock_id | INT | TTLock锁ID |
| lock_name | VARCHAR(100) | 锁蓝牙名称 |
| lock_alias | VARCHAR(100) | 锁别名（用户自定义） |
| lock_mac | VARCHAR(50) | 锁MAC地址 |
| lock_data | TEXT | 管理员锁数据 |
| key_id | INT | 管理员钥匙ID |
| electric_quantity | INT | 电量 |
| keyboard_pwd_version | INT | 密码版本 |
| special_value | INT | 特征值 |
| lock_version | TEXT | 锁版本信息 |
| group_id | INT | 分组ID |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 5.3 电子钥匙表 (ekey)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 本系统用户ID（钥匙拥有者） |
| lock_id | BIGINT | 关联锁记录ID |
| key_id | INT | TTLock钥匙ID |
| lock_data | TEXT | 钥匙对应的锁数据 |
| key_name | VARCHAR(100) | 钥匙名称 |
| key_type | VARCHAR(20) | owner=拥有者, common=普通 |
| start_date | BIGINT | 有效期开始（毫秒时间戳） |
| end_date | BIGINT | 有效期结束 |
| remarks | VARCHAR(500) | 备注 |
| remote_enable | INT | 远程开锁权限（⚠️ 本期暂不实现，字段保留） |
| status | VARCHAR(20) | active/expired/deleted |
| created_at | DATETIME | 创建时间 |

### 5.4 密码表 (passcode)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 所属用户ID |
| lock_id | BIGINT | 关联锁记录ID |
| keyboard_pwd_id | INT | TTLock密码ID |
| keyboard_pwd | VARCHAR(20) | 密码值（加密存储） |
| keyboard_pwd_name | VARCHAR(100) | 密码名称 |
| pwd_type | INT | 密码类型：1-单次 2-永久 3-限时 4-删除 5~14-循环 |
| is_custom | TINYINT | 是否自定义：1-是，0-随机 |
| start_date | BIGINT | 有效期开始 |
| end_date | BIGINT | 有效期结束 |
| status | INT | 1-正常 2-过期 |
| created_at | DATETIME | 创建时间 |

### 5.5 操作记录表 (record)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 操作用户ID |
| lock_id | BIGINT | 关联锁ID |
| action | VARCHAR(20) | unlock=开锁, lock=关锁 |
| result | VARCHAR(20) | success=成功, fail=失败 |
| record_time | BIGINT | 操作时间戳 |
| created_at | DATETIME | 创建时间 |

---

## 六、核心业务流程

### 6.1 用户注册流程（用户无感知TTLock）

```
用户打开小程序 → 点击注册
输入手机号/邮箱 + 验证码(666666) + 密码
         │
         ▼
小程序 → POST /api/user/register
         │
         ▼
本系统后端:
  1. 校验手机号/邮箱唯一性
  2. 校验验证码 = 666666
  3. 密码加密存储
  4. 调用 TTLock /v3/user/register 创建TTLock账号 ← 用户不知晓
     - ⚠️ TTLock 会自动给用户名加前缀（如 `gegjb_`），需保存返回的带前缀用户名
     - ⚠️ TTLock 注册和获取token均要求密码为 32位小写 MD5
     - ⚠️ 注册失败或获取token失败应抛异常回滚，不应静默继续
  5. 保存 tt_username（带前缀）, tt_password 到数据库
  6. 返回 JWT Token + 用户基本信息（无TTLock信息）
         │
         ▼
用户注册成功 → 进入锁列表页
```

### 6.2 添加锁流程

```
用户点击"添加锁" → 进入搜索页面
         │
         ▼
小程序调用 plugin.startScanBleDevice() 搜索蓝牙设备
显示设备列表（名称、信号强度）
         │
         ▼
用户选择要添加的设备（isSettingMode == true 的）
         │
         ▼
小程序调用 plugin.initLock() 蓝牙初始化
    → 返回 lockData
         │
         ▼
小程序 → POST /api/lock/add { lockData, lockName, lockMac, ... }
         │
         ▼
本系统后端:
  1. 调用 TTLock /v3/lock/initialize 云端初始化
     → 返回 lockId, keyId
  2. 保存锁记录到数据库
  3. 保存管理员钥匙记录到数据库
  4. 返回锁信息给小程序
         │
         ▼
小程序提示"添加成功" → 跳转锁列表
```

### 6.3 开锁/关锁流程

#### 蓝牙开锁（拥有者/管理员）
```
用户点击某把锁的"开锁"
         │
         ▼
小程序 → GET /api/lock/detail (获取lockData)
         │
         ▼
小程序调用 plugin.controlLock({ controlAction:3, lockData })
    → 蓝牙连接锁 → 开锁成功/失败
         │
         ▼
小程序 → POST /api/record/upload (上传操作记录)
         │
         ▼
界面显示开锁结果
```

#### 基于电子钥匙开锁（收到钥匙的普通用户）
```
用户进入"我的钥匙" → 选择一把钥匙 → 点击"开门"
         │
         ▼
小程序 → GET /api/key/detail (获取钥匙对应的lockData)
         │
         ▼
小程序调用 plugin.controlLock({ controlAction:3, lockData })
    → 蓝牙连接锁 → 开锁成功/失败
         │
         ▼
记录开锁记录
```

#### 远程开锁（需网关支持）⚠️ 暂不实现
```
⚠️ 本期暂不实现，需网关硬件支持，后续版本考虑

用户点击"远程开锁"
         │
         ▼
小程序 → POST /api/lock/remoteUnlock { lockId }
         │
         ▼
本系统后端:
  调用 TTLock /v3/lock/unlock 远程开锁
    → 返回结果
         │
         ▼
返回结果给小程序
```

### 6.4 发送电子钥匙流程

```
锁拥有者选择一把锁 → 点击"发送钥匙"
         │
         ▼
输入接收方手机号/邮箱
选择钥匙类型：永久/限时/单次/循环
设置有效期
         │
         ▼
小程序 → POST /api/key/send
  { receiverUsername(手机号/邮箱), lockId, keyName, startDate, endDate, remarks }
         │
         ▼
本系统后端:
  1. 查询接收方是否已注册本系统
     - 如已注册：获取其tt_username
     - 如未注册：用其手机号作为tt_username (配合 createUser=1)
  2. 调用 TTLock /v3/key/send { receiverUsername, lockId, ..., createUser=1 }
     → 返回 keyId
  3. 保存钥匙记录到数据库
  4. 返回成功
         │
         ▼
小程序提示"发送成功"
接收方刷新钥匙列表即可看到新钥匙
```

### 6.5 密码获取流程

#### 自定义密码（蓝牙写入）
```
用户选择"添加自定义密码"
         │
         ▼
输入密码值(4-9位数字)，设置名称和有效期
         │
         ▼
小程序调用 plugin.createCustomPasscode({ passcode, startDate, endDate, lockData })
    → 蓝牙写入锁成功
         │
         ▼
小程序 → POST /api/pwd/custom/add { lockId, keyboardPwd, startDate, endDate, name }
         │
         ▼
本系统后端:
  调用 TTLock /v3/keyboardPwd/add 同步云端
    → 返回 keyboardPwdId
  保存到数据库
         │
         ▼
小程序提示"添加成功"
```

#### 离线随机密码（无需蓝牙）
```
用户选择"获取密码" → 选择类型(限时/单次/永久/循环/删除)
设置名称和有效期
         │
         ▼
小程序 → POST /api/pwd/generate { lockId, pwdType, startDate, endDate, name }
         │
         ▼
本系统后端:
  调用 TTLock /v3/keyboardPwd/get 获取随机密码
    → 返回 keyboardPwd + keyboardPwdId
  保存到数据库
  返回密码值给小程序
         │
         ▼
小程序展示密码（可直接在锁面板输入使用）
```

### 6.6 重置密码流程
```
用户选择"重置密码"（确认操作）
         │
         ▼
小程序调用 plugin.resetPasscode({ lockData })
    → 蓝牙重置成功 → 返回新lockData
         │
         ▼
小程序 → POST /api/pwd/reset { lockId, newLockData, pwdInfo, timestamp }
         │
         ▼
本系统后端:
  1. 调用 TTLock /v3/lock/resetKeyboardPwd 上传密码数据
  2. 调用 TTLock /v3/lock/update 更新锁数据
  3. 删除本系统该锁所有密码记录
  4. 更新锁的lockData
  5. 返回成功
         │
         ▼
小程序提示"重置成功，所有旧密码已失效"
```

### 6.7 删除锁流程
```
用户选择"删除锁"（确认操作）
         │
         ▼
小程序调用 plugin.resetLock({ lockData })
    → 蓝牙重置智能锁（锁恢复到可初始化状态）
         │
         ▼
小程序 → POST /api/lock/delete { lockId }
         │
         ▼
本系统后端:
  1. 删除本系统该锁的所有密码记录
  2. 删除本系统该锁的所有钥匙记录
  3. 删除本系统该锁记录
  （TTLock云端通过resetLock已自动解除绑定，无需额外调用）
         │
         ▼
小程序提示"删除成功" → 刷新锁列表
```

### 6.8 删除钥匙流程
```
用户选择一把钥匙 → 点击"删除"
         │
         ▼
小程序 → POST /api/key/delete { keyId }
         │
         ▼
本系统后端:
  1. 调用 TTLock /v3/key/delete 删除云端钥匙
  2. 删除本系统钥匙记录
  3. 返回成功
         │
         ▼
小程序提示"删除成功"
```

---

### 6.9 通用接口规范

#### 统一返回结构

所有接口返回统一JSON格式：

```json
{
  "code": 0,
  "message": "success",
  "data": {}
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| code | Int | 0=成功，非0=失败 |
| message | String | 错误描述（成功时为"success"） |
| data | Object/Array/null | 响应数据（失败时可为null） |

分页接口返回结构：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [],
    "total": 100,
    "pageNo": 1,
    "pageSize": 20
  }
}
```

#### 错误码定义

| 错误码 | 说明 | HTTP状态码 |
|--------|------|-----------|
| 0 | 成功 | 200 |
| 1001 | 参数错误（必填缺失/格式错误） | 400 |
| 1002 | 未登录或Token过期 | 401 |
| 1003 | 无权限操作 | 403 |
| 1004 | 资源不存在 | 404 |
| 2001 | 用户名已存在 | 409 |
| 2002 | 验证码错误 | 400 |
| 2003 | 账号或密码错误 | 401 |
| 3001 | TTLock用户注册失败 | 502 |
| 3002 | TTLock令牌获取失败 | 502 |
| 3003 | 锁不存在 | 404 |
| 3004 | 锁已被其他用户绑定 | 409 |
| 3005 | 无该锁的管理权限 | 403 |
| 4001 | 钥匙不存在 | 404 |
| 4002 | 钥匙已过期 | 410 |
| 4003 | 无该钥匙的使用权限 | 403 |
| 5001 | 密码不存在 | 404 |
| 5002 | 密码已过期 | 410 |
| 6001 | TTLock服务调用失败 | 502 |
| 6002 | TTLock服务超时 | 504 |
| 6003 | TTLock令牌失效，需重新授权 | 502 |
| 9001 | 系统内部错误 | 500 |
| 9002 | 请求过于频繁 | 429 |

#### 参数校验规则

| 参数 | 校验规则 |
|------|---------|
| 手机号 | 11位数字，/^1[3-9]\d{9}$/ |
| 邮箱 | 合法邮箱格式，/^[\w.-]+@[\w.-]+\.\w+$/ |
| 密码 | 6-20位，至少包含字母和数字 |
| 验证码 | 6位数字 |
| lockId | 正整数 |
| pageNo | ≥1 |
| pageSize | 1-100，默认20 |
| keyName | 1-50个字符 |
| 密码值(自定义) | 4-9位数字 |
| 锁别名 | 1-20个字符 |
| startDate/endDate | 毫秒时间戳，endDate > startDate |

#### 幂等性处理

| 接口 | 幂等策略 |
|------|---------|
| POST /api/user/register | 手机号/邮箱唯一索引防重 |
| POST /api/lock/add | 前端按钮禁用 + 后端检查lockId是否已存在 |
| POST /api/key/send | 前端按钮禁用 + 后端检查是否已发送相同钥匙 |
| POST /api/pwd/generate | 前端按钮禁用 + 后端每次生成唯一密码 |
| POST /api/pwd/delete | 删除操作天然幂等 |
| POST /api/lock/delete | 删除操作天然幂等 |
| POST /api/record/upload | record_time + lock_id + action 唯一索引防重 |

#### 异常处理策略

| 场景 | 处理方式 |
|------|---------|
| TTLock API超时 | 重试1次（间隔500ms），仍失败则返回6002 |
| TTLock API返回错误 | 记录日志，返回6001+原错误信息 |
| TTLock API调用成功但本地写入失败 | 记录待同步任务，后台重试，返回成功给前端 |
| JWT过期 | 返回1002，前端引导重新登录 |
| 并发冲突（如同时删除同一锁） | 乐观锁/版本号校验，冲突时返回提示 |

---

### 6.10 补充接口实现逻辑

#### 用户模块

##### 发送验证码（POST /api/user/sendCode）

```
小程序 → POST /api/user/sendCode { username(手机号/邮箱) }
         │
         ▼
后端:
  1. 校验username格式（手机号或邮箱）
  2. 限流校验：
     - 同一手机号/邮箱：1分钟内最多1次
     - 同一IP：每小时最多10次
  3. 生成6位验证码
  4. 存入Redis：key=code:{username}，value=验证码，expire=300秒
  5. 返回成功（固定返回666666，无需实际发送短信）
         │
         ▼
返回: { "code": 0, "message": "success" }
```

##### 用户登录（POST /api/user/login）

```
小程序 → POST /api/user/login { username, password }
         │
         ▼
后端:
  1. 校验username、password非空
  2. 根据username查询用户
     - 不存在 → 返回2003（账号或密码错误）
  3. 校验密码（BCrypt比对）
     - 不匹配 → 返回2003（账号或密码错误）
  4. 生成JWT Token（有效期7天）
     - payload: { userId, username, exp }
  5. 返回用户信息 + Token
         │
         ▼
返回:
{
  "code": 0,
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "userInfo": {
      "userId": 1,
      "username": "13800138000",
      "nickname": "用户",
      "avatar": "https://..."
    }
  }
}
```

##### 获取用户信息（GET /api/user/info）

```
小程序 → GET /api/user/info (Header: Authorization: Bearer {token})
         │
         ▼
后端:
  1. 解析JWT Token，获取userId
     - Token无效/过期 → 返回1002
  2. 查询用户表
  3. 返回用户信息（**不包含TTLock相关字段**）
         │
         ▼
返回:
{
  "code": 0,
  "data": {
    "userId": 1,
    "username": "13800138000",
    "nickname": "用户",
    "avatar": "https://...",
    "createdAt": "2024-01-01 00:00:00"
  }
}
```

---

#### 锁模块

##### 锁列表（GET /api/lock/list）

```
小程序 → GET /api/lock/list?pageNo=1&pageSize=20
         │
         ▼
后端:
  1. 解析Token获取userId
  2. 查询锁表（两部分合并）：
     a. 我创建的锁：WHERE user_id = userId
     b. 别人分享给我的：通过ekey表关联查询
        WHERE ekey.user_id = userId AND ekey.key_type = 'common'
  3. 合并去重，按创建时间倒序
  4. 分页返回
         │
         ▼
返回:
{
  "code": 0,
  "data": {
    "list": [
      {
        "lockId": 123,
        "lockName": "蓝牙名称",
        "lockAlias": "用户自定义名称",
        "lockMac": "AA:BB:CC:DD:EE:FF",
        "electricQuantity": 100,
        "lockData": "xxx",
        "keyType": "owner",
        "hasGateway": 0
      }
    ],
    "total": 5,
    "pageNo": 1,
    "pageSize": 20
  }
}
```

##### 锁详情（GET /api/lock/detail）

```
小程序 → GET /api/lock/detail?lockId=123
         │
         ▼
后端:
  1. 解析Token获取userId
  2. 查询锁表 WHERE id = lockId
     - 不存在 → 返回3001
  3. 权限校验：
     - 检查userId是否为该锁的user_id（拥有者）
     - 或通过ekey表检查userId是否持有该锁的有效钥匙
     - 无权限 → 返回3003
  4. 返回锁详情
         │
         ▼
返回:
{
  "code": 0,
  "data": {
    "lockId": 123,
    "lockName": "蓝牙名称",
    "lockAlias": "用户自定义名称",
    "lockMac": "AA:BB:CC:DD:EE:FF",
    "electricQuantity": 85,
    "lockData": "xxx",
    "keyId": 789,
    "keyType": "owner",
    "keyboardPwdVersion": 4,
    "specialValue": 1288,
    "lockVersion": "v1.0",
    "hasGateway": 0
  }
}
```

##### 更新锁数据（POST /api/lock/updateData）

```
小程序 → POST /api/lock/updateData { lockId, lockData }
         │
         ▼
后端:
  1. 解析Token获取userId
  2. 校验参数：lockId、lockData非空
  3. 查询锁表，校验权限（必须为拥有者）
     - 不存在 → 返回3001
     - 无权限 → 返回3003
  4. 调用TTLock /v3/lock/updateLockData
     - 失败 → 返回6001
  5. 更新本地锁表lock_data字段
  6. 返回成功
         │
         ▼
返回: { "code": 0, "message": "success" }
```

---

#### 钥匙模块

##### 钥匙列表（GET /api/key/list）

```
小程序 → GET /api/key/list?pageNo=1&pageSize=20
         │
         ▼
后端:
  1. 解析Token获取userId
  2. 查询ekey表 WHERE user_id = userId
  3. 关联查询锁表获取锁名称、MAC等信息
  4. 按创建时间倒序
  5. 分页返回
         │
         ▼
返回:
{
  "code": 0,
  "data": {
    "list": [
      {
        "keyId": 456,
        "lockId": 123,
        "lockName": "前门锁",
        "lockAlias": "大门",
        "lockMac": "AA:BB:CC:DD:EE:FF",
        "electricQuantity": 85,
        "keyType": "owner",
        "keyName": "管理员钥匙",
        "startDate": 1704067200000,
        "endDate": 0,
        "remarks": "",
        "remoteEnable": 1,
        "status": "active"
      }
    ],
    "total": 3,
    "pageNo": 1,
    "pageSize": 20
  }
}
```

##### 钥匙详情（GET /api/key/detail）

```
小程序 → GET /api/key/detail?keyId=456
         │
         ▼
后端:
  1. 解析Token获取userId
  2. 查询ekey表 WHERE id = keyId
     - 不存在 → 返回4001
  3. 权限校验：ekey.user_id = userId
     - 无权限 → 返回4003
  4. 关联查询锁表获取lockData等信息
  5. 检查有效期：
     - endDate ≠ 0 且 endDate < 当前时间 → status = "expired"
  6. 返回钥匙详情（含lockData，用于蓝牙开锁）
         │
         ▼
返回:
{
  "code": 0,
  "data": {
    "keyId": 456,
    "lockId": 123,
    "lockData": "xxx",
    "lockName": "前门锁",
    "lockAlias": "大门",
    "lockMac": "AA:BB:CC:DD:EE:FF",
    "electricQuantity": 85,
    "keyType": "common",
    "keyName": "访客钥匙",
    "startDate": 1704067200000,
    "endDate": 1704153600000,
    "remarks": "保洁阿姨",
    "remoteEnable": 2,
    "status": "active"
  }
}
```

---

#### 密码模块

##### 密码列表（GET /api/pwd/list）

```
小程序 → GET /api/pwd/list?lockId=123&pageNo=1&pageSize=20
         │
         ▼
后端:
  1. 解析Token获取userId
  2. 校验lockId参数
  3. 权限校验：检查用户是否有该锁的访问权限（拥有者或钥匙持有者）
     - 无权限 → 返回3003
  4. 查询passcode表 WHERE lock_id = lockId
  5. 过滤已过期的密码（end_date < 当前时间 且 pwd_type ≠ 2永久）
  6. 按创建时间倒序
  7. 分页返回
         │
         ▼
返回:
{
  "code": 0,
  "data": {
    "list": [
      {
        "pwdId": 789,
        "keyboardPwdId": 234,
        "keyboardPwd": "123456",
        "keyboardPwdName": "临时密码",
        "pwdType": 3,
        "pwdTypeName": "限时",
        "isCustom": 1,
        "startDate": 1704067200000,
        "endDate": 1704153600000,
        "status": 1
      }
    ],
    "total": 2,
    "pageNo": 1,
    "pageSize": 20
  }
}
```

##### 删除密码（POST /api/pwd/delete）

```
小程序 → POST /api/pwd/delete { lockId, pwdId }
         │
         ▼
后端:
  1. 解析Token获取userId
  2. 校验参数：lockId、pwdId非空
  3. 查询passcode表 WHERE id = pwdId AND lock_id = lockId
     - 不存在 → 返回5001
  4. 权限校验：检查用户是否为该锁的拥有者
     - 无权限 → 返回4003
  5. 调用TTLock /v3/keyboardPwd/delete
     - 失败 → 记录日志，继续删除本地（蓝牙已删除，云端同步可异步）
  6. 删除本地passcode记录
  7. 返回成功
         │
         ▼
返回: { "code": 0, "message": "success" }
```

---

#### 记录模块

##### 记录列表（GET /api/record/list）

```
小程序 → GET /api/record/list?lockId=123&pageNo=1&pageSize=20&startDate=&endDate=
         │
         ▼
后端:
  1. 解析Token获取userId
  2. 校验lockId参数
  3. 权限校验：检查用户是否有该锁的访问权限
     - 无权限 → 返回3003
  4. 查询record表 WHERE lock_id = lockId
  5. 可选：按时间范围筛选（startDate/endDate）
  6. 按操作时间倒序
  7. 分页返回
         │
         ▼
返回:
{
  "code": 0,
  "data": {
    "list": [
      {
        "recordId": 1001,
        "lockId": 123,
        "lockAlias": "大门",
        "action": "unlock",
        "actionName": "开锁",
        "result": "success",
        "recordTime": 1704067200000,
        "createdAt": "2024-01-01 10:00:00"
      }
    ],
    "total": 50,
    "pageNo": 1,
    "pageSize": 20
  }
}
```

---

## 七、页面功能清单（用户视角命名）

| 页面 | 路径 | 功能描述 |
|------|------|---------|
| 登录页 | pages/login/index | 手机号/邮箱 + 密码登录 |
| 注册页 | pages/register/index | 注册（手机号/邮箱 + 验证码666666 + 密码） |
| 首页-锁列表 | pages/lock-list/index | 展示我的所有锁（我创建的 + 别人分享的） |
| 添加锁-搜索 | pages/lock-add/search | 蓝牙搜索附近智能锁设备 |
| 添加锁-确认 | pages/lock-add/confirm | 确认添加选中的设备 |
| 锁控制页 | pages/lock-control/index | 开锁/关锁、查看电量、操作入口 |
| 钥匙列表页 | pages/key-list/index | 我的所有电子钥匙 |
| 发送钥匙页 | pages/key-send/index | 选择接收人、钥匙类型、有效期 |
| 密码列表页 | pages/pwd-list/index | 锁的所有密码 |
| 获取密码页 | pages/pwd-get/index | 选择类型生成随机密码 |
| 添加自定义密码页 | pages/pwd-custom/index | 输入自定义密码和有效期 |
| 开锁记录页 | pages/record-list/index | 操作记录历史 |

---

## 八、技术栈建议

| 层次 | 技术 | 说明 |
|------|------|------|
| 小程序框架 | 微信原生小程序 | WXML + WXSS + JS/TS |
| UI组件 | Vant Weapp | 轻量、美观的小程序UI库 |
| 状态管理 | 全局App + 本地存储(storage) | 简单够用 |
| 后端语言 | Java (Spring Boot) / Node.js | 建议与团队技术栈匹配 |
| 数据库 | MySQL + Redis | 关系型存储 + 缓存/验证码 |
| HTTP客户端 | wx.request | 小程序端请求后端 |
| 蓝牙通信 | TTLock插件 wx43d5971c94455481 @ 3.1.6 | 仅用于蓝牙（对内） |
| 认证鉴权 | JWT | 小程序与后端之间的认证 |
| API文档 | Swagger / Knife4j | 后端接口文档 |

---

## 九、用户界面文案对照表

> 所有用户可见的UI文案中，使用左侧的"用户术语"，避免使用右侧的"技术术语"

| 用户术语（对外） | 技术术语（对内/后端） |
|---|---|
| 智能锁 / 设备 | TTLock Lock |
| 管理员 / 拥有者 | Admin ekey |
| 分享给我的 / 收到的钥匙 | Common ekey |
| 开门密码 / 键盘密码 | Keyboard Passcode |
| 生成密码 | keyboardPwd/get |
| 开锁 | unlock / controlLock(3) |
| 关锁 | lock / controlLock(6) |
| 分享钥匙 / 发送钥匙 | key/send |
| 删除钥匙 | key/delete |
| 添加设备 | lock/initialize |
| 重置设备 | resetLock |
| 账号 | TTLock username |
| 我的 | 无对应（我们自己的用户体系） |

---

## 十、TTLock API 开发注意事项（踩坑记录）

> 以下是在对接 TTLock Cloud API V3 过程中遇到的关键问题，开发时务必注意。

### 10.1 用户名自动前缀

- **现象**: 调用 `/v3/user/register` 注册用户时，TTLock 会自动给用户名添加前缀（如 `gegjb_`）
- **示例**: 本地生成用户名 `app7dc84420516241b`，TTLock 返回 `gegjb_app7dc84420516241b`
- **影响**: 后续所有 TTLock API 调用（获取token、调用锁接口等）必须使用**返回的带前缀用户名**
- **正确做法**: 注册时保存 TTLock 返回的完整用户名（带前缀），获取token时使用该用户名
- **错误做法**: 使用本地生成的用户名（不带前缀）获取token → 返回 `10007 invalid account or invalid password`

### 10.2 密码 MD5 加密要求

- **现象**: TTLock 所有接口（包括注册和获取token）均要求密码为 **32位小写 MD5** 加密
- **正确做法**:
  ```java
  String md5Password = DigestUtils.md5DigestAsHex(password.getBytes(StandardCharsets.UTF_8));
  ```
- **错误做法**: 使用明文密码 → 返回 `30005 password must be md5 encrypted`

### 10.3 注册与Token获取必须成功

- **现象**: TTLock 注册成功后，如果获取token失败，用户数据不应入库
- **正确做法**: 注册和获取token必须成功，失败应抛异常回滚用户注册流程
- **错误做法**: 静默继续 → 用户以为注册成功，但实际无法使用任何TTLock功能

### 10.4 错误码参考

| 错误码 | 含义 | 常见原因 |
|--------|------|----------|
| 10007 | invalid account or invalid password | 用户名不带前缀 / 密码未MD5 / 账号不存在 |
| 30002 | invalid username | 用户名包含非法字符（如下划线） |
| 30005 | password must be md5 encrypted | 密码未MD5加密 |
| 30006 | username already exists | 用户名已存在 |
